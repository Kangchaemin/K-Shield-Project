프로그램과 프로세스의 차이. 

동적 분석같은 경우 메모리에서 분석할때마다 다른곳에 메모리를 올리기 때문에 주소가 당연히 다르다. 
-> 메모리 매핑할때는 메모리 재배치가 필요하다. 

바이너리 상태 그대로 분석하기 때문에 정적분석같은 경우는 당연하게도 주소가 바뀌지 않는다. 
->메모리 상태에서의 재배치가 필요가 없다. 

: 메모리 주소를 바꿔주는것만으로도 악성코드를 방지할 수 있다?

Q: 패킹이 되어있지 않는 닷넷 악성코드의 경우에는 디컴파일러를 활용하면 쉽게 분석이 간으했습니다.
다만, Agentesla나 Formbook과 같은 최신 악성코드의 경우에는 패킹이 되어있고 리소스를 로드한 뒤,
복호화해 악성스크립트를 실행하는 형태가 많은것으로 알고있습니다.  이러한 닷넷 악성코드 분석 시
EntryPoint에서부터 트레이싱하다보면 어느 순간 악성 행위들이 실해오디서 제대로된 분석을 하지 못한다.

A: Main을 찾는 것은 중요하지 않다. Main에 진입하기 위한 것들을 판단하기 위한게 크다. 
